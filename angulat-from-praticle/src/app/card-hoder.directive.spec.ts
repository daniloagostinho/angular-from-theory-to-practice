import { CardHoderDirective } from './card-hoder.directive';

describe('CardHoderDirective', () => {
  it('should create an instance', () => {
    const directive = new CardHoderDirective();
    expect(directive).toBeTruthy();
  });
});
